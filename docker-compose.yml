services:
  bot:
    build: .
    restart: on-failure
    tty: true
    stdin_open: true
    env_file:
      - bot-app/.env
    environment:
      - TZ=Europe/Moscow

    extra_hosts:
      - "host.docker.internal:host-gateway"